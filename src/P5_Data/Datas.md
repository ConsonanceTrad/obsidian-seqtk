## 职责

- SQLite 的接入逻辑
- 缓存、文件的规范操作逻辑
- 读写的顺序编排
- 数据和其他层的套接层逻辑

## 边界判据

放这里：
- 读写文件、db 
- 建立和传递内存缓存
- 队列编排；解析与序列化
- 数据交互的套接层

不放这里：

- 纯解析、纯计算（不依赖 obsidian）→ `Tools`
- 带业务规则的节点操作 → `P4_Nodes`
- 任何 UI / 渲染
